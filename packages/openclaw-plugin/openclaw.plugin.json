{
    "id": "prometheus-avatar",
    "name": "Prometheus Avatar",
    "version": "0.10.0",
    "description": "Give your OpenClaw agent a Live2D avatar (lip-sync · emotion · TTS) plus AAA image generation for marketplace skin previews. 3 agent tools + bundled skill.",
    "skills": [
        "skills/prometheus-avatar"
    ],
    "type": "tool",
    "author": "Myths Labs",
    "license": "MIT",
    "repository": "https://github.com/myths-labs/prometheus",
    "configSchema": {
        "avatarId": {
            "type": "string",
            "description": "Avatar model ID from Prometheus Marketplace (or URL to a .model3.json file)",
            "required": false
        },
        "modelUrl": {
            "type": "string",
            "description": "Direct URL to a Live2D model file (.model3.json)",
            "required": false
        },
        "ttsProvider": {
            "type": "string",
            "deprecated": true,
            "description": "DEPRECATED in v0.10.0 — this field is ignored. TTS is now delegated to the Prometheus SDK (@prometheusavatar/core) which uses the Prometheus voice backend. Kept for backward compat only."
        },
        "ttsVoice": {
            "type": "string",
            "description": "Prometheus Marketplace voice ID (e.g. saturn_zh_female_keainvsheng_tob). Browse available voices at prometheus.mythslabs.ai/marketplace.",
            "required": false
        },
        "enableLipSync": {
            "type": "boolean",
            "default": true,
            "description": "Enable audio-driven lip synchronization"
        },
        "enableEmotion": {
            "type": "boolean",
            "default": true,
            "description": "Enable emotion analysis from text"
        }
    },
    "events": {
        "listens": [
            "agent:message",
            "agent:thinking",
            "agent:error"
        ],
        "emits": [
            "avatar:speak",
            "avatar:emotion",
            "avatar:ready"
        ]
    }
}